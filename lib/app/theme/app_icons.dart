import 'package:flutter/material.dart';

import 'app_colors.dart';

class AppIcons {
  AppIcons._();

  static const Widget arrowBack = Icon(
    Icons.arrow_back_ios,
    color: AppColors.orange,
  );

  static const Widget settings = Icon(
    Icons.settings,
    color: AppColors.orange,
  );

  static const Widget add = Icon(
    Icons.add,
    color: AppColors.orange,
  );

  static const Widget remove = Icon(
    Icons.remove,
    color: AppColors.orange,
  );

  static const Widget arrowUpward = Icon(
    Icons.arrow_upward,
    color: AppColors.orange,
  );

  static const Widget editPhoto = Icon(
    Icons.photo,
    color: AppColors.white,
    size: 40,
  );

  static const Widget edit = Icon(
    Icons.edit,
    color: AppColors.orange,
  );

  static const Widget delete = Icon(
    Icons.delete,
    color: AppColors.red,
  );
}
